// ORR (||) operator

// let hours = 12;
// let me = "hello";

// if(hours < 9|| hours >13);
// alert("hello");



// const me = '134';
// const you = 12;

// if(me  > you || me < you || 'sorry'){
//     alert("office is open");
// };

// alert(true || true) || 'hello';    // agar statment ka ak be side true ha to value retrun hoge 
// alert( true || false || ' hello');
// alert(false || true || 'hello');              
// alert(false || false || 'hello');         // agar statment ka dono sides false ho to third value run hoge 




// Evaluates operands from left to right.
// For each operand, converts it to a boolean. If the result is false, stops and returns the original value of that operand.
// If all operands have been evaluated (i.e. all were truthy), returns the last operand.